# This workflow is disabled because GitHub Projects built-in automation is enabled at:
# https://github.com/orgs/ministryofjustice/projects/152/workflows/86554984
# The GITHUB_TOKEN does not have permissions to write to org-level projects.
# 
# If you need to re-enable this workflow, you must:
# 1. Create a PAT with 'project' and 'repo' scopes
# 2. Add it as a repository secret (e.g., ADD_TO_PROJECT_PAT)
# 3. Replace secrets.GITHUB_TOKEN with secrets.ADD_TO_PROJECT_PAT

# name: Add Issue to Project

# on:
#   issues:
#     types:
#       - opened

# permissions: {}

# jobs:
#   add-to-project:
#     name: Add issue to project
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       issues: read
#     steps:
#       - name: Add issue to project
#         uses: actions/add-to-project@244f685bbc3b7adfa8466e08b698b5577571133e # v1.0.2
#         with:
#           project-url: https://github.com/orgs/ministryofjustice/projects/152
#           github-token: ${{ secrets.GITHUB_TOKEN }}
